meta {
  name: Get tasks
  type: http
  seq: 2
}

get {
  url: {{API_URL}}/api/taskeritems
  body: none
  auth: inherit
}

assert {
  res.body.length: gte 1
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
