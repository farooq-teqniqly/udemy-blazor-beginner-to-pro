meta {
  name: Delete task
  type: http
  seq: 4
}

delete {
  url: {{API_URL}}/api/taskeritems/2
  body: json
  auth: inherit
}

assert {
  res.status: eq 204
}

script:pre-request {
  const taskNames = ["Wash dishes", "Write code", "Exercise", "Read a book", "Buy groceries"];
  const index = Math.floor(Math.random() * taskNames.length);
  
  bru.setVar("TASK_NAME", taskNames[index]);
}

settings {
  encodeUrl: true
  timeout: 0
}
