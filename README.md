# Quick Setup (PowerShell)

If you are on Windows, run this command in your terminal to generate the required secrets with default values:

```powershell

```

After running these commands, you can start the environment with docker compose up -d.
